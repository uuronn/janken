import { css } from '@emotion/react'

const sm = css`
  width: 16px;
  height: 16px;
`

const md = css`
  width: 20px;
  height: 20px;
`

const lg = css`
  width: 40px;
  height: 40px;
`

export const sizeTheme = {
  sm,
  md,
  lg
}
